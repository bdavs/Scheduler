C#
To compile, you will need a .NET runtime environment.  
For example, using Visual Studio, open the .sln and compile solution.  
Then run it through the visual studio tools or run the .exe in the bin/debug folder
The input_file.txt contains the input data.  format is number of processes, quantum number, data